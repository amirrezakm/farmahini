// Temporarily disabled internationalization middleware
// This will be re-enabled once we get the basic site working

export function middleware() {
  // No middleware for now - direct routing
  return;
}

export const config = {
  matcher: []
};